spring:
  application:
    name: todo-backend
  profiles:
    active: local

  jpa:
    properties:
      hibernate:
        default_schema: todolist_db
        format_sql: true

logging:
  level:
    org.hibernate: info
